import React from "react"

const Footer = () => {
    return (
        <footer className="footer">
            <p>&copy; {new Date().getFullYear()} Travel Planner. All right reserved.</p>
        </footer>
    );
};

export default Footer;